

# Rename VentureAI → PitchScore

## Changes

Two files contain "VentureAI" references:

1. **`src/pages/Auth.tsx`** (line 125): Change "VentureAI" to "PitchScore"
2. **`src/pages/Dashboard.tsx`** (line 528): Change "VentureAI" to "PitchScore"

Also update the subtitle in Auth.tsx (line 129) from "AI-Powered Startup Pitch Analysis" to "AI-Powered Pitch Intelligence" to match the landing page branding.

Additionally, update `index.html` page title if it references VentureAI.

