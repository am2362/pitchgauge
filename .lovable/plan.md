

# Rename PitchScore → PitchGauge + Update Pricing

## Changes

### 1. `index.html`
- Replace "PitchScore" with "PitchGauge" in title and OG tags

### 2. `src/pages/Landing.tsx`
- Replace all "PitchScore" → "PitchGauge" (nav, mockup URL, footer, copyright)
- Change Pro price from `$19` to `$29`
- Change Scale price from `$49` to `$89`

### 3. `src/pages/Auth.tsx`
- Replace "PitchScore" → "PitchGauge"

### 4. `src/pages/Dashboard.tsx`
- Replace "PitchScore" → "PitchGauge"

All straightforward find-and-replace operations across 4 files.

